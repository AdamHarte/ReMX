<!-- Defines a resource package.                                             -->
<!-- The name of the SWF file used by this resource package MUST match the   -->
<!-- name of this XML file. For example, demo.xml must have a demo.swf file. -->
<resource>

	<!-- NOTE                                                               -->
	<!-- Images within the SWF library MUST have and width and height which -->
	<!-- are a power of two. The following values are allowed:              -->
	<!-- 2, 4, 8, 16, 32, 64, 128, 256, 512, 1024                           -->
	<!-- However, the image can be rectangular, so the width could be 512   -->
	<!-- and the height could be 256, or vise versa.                        -->


	<!-- Defines a set of tiles for use with a grid graphic.                    -->
	<!-- id        = A unique identifier for this tileset.           [required] -->
	<!-- source    = The class name of the image in the SWF library. [required] -->
	<!-- width     = The nominal width of one tile, in pixels.       [required] -->
	<!-- height    = The nominal height of one tile, in pixels.      [required] -->
	<tileset id="floor.tiles" source="tiles.png" width="32" height="32">
		<!-- At least one tile element must be created for each tileset. -->
		<!-- x = The position of this tile within the 'source' image, in pixels. [required] -->
		<!-- y = The position of this tile within the 'source' image, in pixels. [required] -->
		<tile x="0"  y="0"/>
		<tile x="32" y="0"/>
		<tile x="0"  y="32"/>
		<tile x="32" y="32"/>
	</tileset>

	<tileset id="background.tiles" source="tiles.png" width="64" height="64">
		<tile x="64" y="0"/>
	</tileset>

</resource>